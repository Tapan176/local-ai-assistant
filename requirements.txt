# Core dependencies
python-dateutil
dateparser
pydantic
rich
pytest
schedule

# Optional: Cognee + Neo4j (for semantic/graph memory)
# Uncomment if using CogneeTool features:
# cognee
# neo4j
speechrecognition
pyttsx3
protego>=0.1
playwright>=1.9.0
