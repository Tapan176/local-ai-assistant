# Core dependencies
python-dateutil
dateparser
pydantic
rich
pytest
schedule

# Optional: Cognee + Neo4j (for semantic/graph memory)
# Uncomment if using CogneeTool features:
# cognee
# neo4j
speechrecognition
pyttsx3
protego>=0.1
playwright>=1.9.0

# TAPAN_AI v2 stack
fastapi>=0.111.0
uvicorn>=0.30.0
httpx>=0.27.0
PyYAML>=6.0.0
chromadb>=0.5.0
openai>=1.40.0
